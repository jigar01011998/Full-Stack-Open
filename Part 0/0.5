sequenceDiagram
    participant browser
    participant server

    browser->>server: GET https://studies.cs.helsinki.fi/exampleapp/spa
    activate server
    server-->>browser: HTML document 
    deactivate server

Note right of browser: The HTML document for the Single-Page Application (SPA) version of the notes app is fetched by the browser. There is no need for any additional requests to fetch separate HTML pages. The execution of JavaScript code in the browser takes care of the application logic and data manipulation.

